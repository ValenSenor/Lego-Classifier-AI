# Lego Franchise Classifier (AI) 🧠

This project uses a Convolutional Neural Network (CNN) to classify Lego figures.

## 📂 Dataset Information
The dataset is **automatically downloaded** using the `kagglehub` library directly within the notebook.
- **Source:** Kaggle
- **Method:** You do not need to download images manually. Just run the notebook, and `kagglehub` will handle the data fetching.

## 🛠 Dependencies
To run this project, install the dependencies:
```bash
pip install -r requirements.txt
